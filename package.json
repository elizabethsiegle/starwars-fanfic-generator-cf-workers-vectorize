{
  "name": "delicate-poetry-9d95",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --assets ./assets",
    "dev": "wrangler dev --assets ./assets --remote",
    "start": "wrangler dev --assets ./assets --remote"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.1.0",
    "@langchain/cloudflare": "^0.0.4",
    "hono": "^4.2.9",
    "langchain": "^0.1.36"
  }
}
